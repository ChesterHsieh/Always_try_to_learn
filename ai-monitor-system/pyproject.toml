[project]
name = "ai-monitor-system"
version = "0.1.0"
description = "Local Kubernetes PySpark monitoring reference"
requires-python = ">=3.11"
dependencies = [
  "pyspark>=3.5.0",
  "opentelemetry-sdk>=1.25.0",
  "opentelemetry-exporter-otlp>=1.25.0",
  "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
