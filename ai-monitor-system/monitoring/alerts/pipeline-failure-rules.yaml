groups:
  - name: pipeline-failure
    rules:
      - alert: PipelineRunFailed
        expr: pipeline_run_total{status="failed"} > 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "PySpark pipeline failure detected"
          dashboard_link: "http://grafana.local/d/pipeline-health"
